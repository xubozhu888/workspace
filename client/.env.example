# Base URL of the backend API (no trailing slash, no /api suffix).
# In production this is your Railway URL; on Vercel set this as an env var.
VITE_API_BASE_URL=https://your-railway-app.up.railway.app
